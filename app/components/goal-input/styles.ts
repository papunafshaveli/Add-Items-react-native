import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  textInput: {
    flex: 2,

    fontSize: 20,
    fontWeight: "bold",

    padding: 10,

    borderWidth: 1,
    borderRadius: 10,
    borderColor: "grey",
  },
});

export default styles;
